# BGP Hijacking Simulation & Detection Lab
# Python 3.8+ required

# Networking & BGP
scapy==2.5.0            # Packet crafting and capture
exabgp==4.2.21          # BGP session management and UPDATE parsing

# Data & Analysis
pandas==2.1.0
matplotlib==3.8.0
numpy==1.25.0
networkx==3.1            # AS topology graph operations

# Database
# SQLite3 is built into Python stdlib — no extra install needed

# Utilities
colorlog==6.8.0          # Colored terminal logging
tabulate==0.9.0          # ASCII table output in CLI
click==8.1.7             # CLI argument parsing
python-dotenv==1.0.0     # Environment variable management
pydantic==2.4.2          # Input validation and data models
flask>=3.0.0             # Web dashboard framework

# Web APIs (Dashboard backend)
fastapi>=0.104.0         # Modern async web framework
uvicorn>=0.24.0          # ASGI server for FastAPI
python-multipart>=0.0.6  # File upload support

# Testing
pytest==7.4.3
pytest-cov==4.1.0
pytest-mock==3.12.0
freezegun==1.2.2         # Freeze time in unit tests

# Optional: Jupyter for interactive analysis
# jupyter==1.0.0
# ipykernel==6.26.0